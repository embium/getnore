# DEV Compose file.
# For a production file see: README.md

services:
  app:
    build: .
    env_file: .env
    depends_on:
      - db
      - redis
    restart: unless-stopped
    ports:
      - 8080:8000
